{
  "name": "monorepo-example",
  "private": true,
  "packageManager": "yarn@3.3.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:nextjs": "turbo run dev --filter=nextjs"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
